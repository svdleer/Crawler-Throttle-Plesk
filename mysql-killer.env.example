# MySQL SELECT overload killer — disabled by default on new servers.
MODE=dry-run
LOAD_THRESHOLD=10
MYSQL_CPU_THRESHOLD=70
REQUIRED_SAMPLES=3
MIN_QUERY_SECONDS=60
# Set the application DB account for the target Plesk subscription.
ALLOWED_USERS=REPLACE_ME
