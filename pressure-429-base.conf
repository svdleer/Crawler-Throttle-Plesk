# Pressure-aware temporary 429 layer. Normal nginx http context only.
# The generated map has empty keys unless Digisteden PHP-FPM is saturated.
include /etc/nginx/pressure-429.map;
limit_req_zone $pressure_429_key zone=pressure429:10m rate=1r/m;
limit_req zone=pressure429 burst=0 nodelay;
